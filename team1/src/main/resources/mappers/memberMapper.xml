<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.local.mappers.memberMapper">

	<!-- 회원가입 -->
	<insert id="insertMember">
  		insert into t_member(mem_id, mem_pw, mem_name, mem_pic)
  				values(#{mem_id}, #{mem_pw}, #{mem_name}, #{mem_pic})
  	</insert>
  	<!-- 아이디 체크 -->
  	<select id="readMember" resultType="int" parameterType="String">
  		select count(*) from t_member
  		where mem_id = #{mem_id}
  	</select>
  	
  	<!-- 로그인 -->
  	<select id="readWithPw" resultType="MemberVo">
  		select * from t_member
  		where mem_id = #{mem_id} and mem_pw = #{mem_pw}
  	</select>
	
</mapper>  
